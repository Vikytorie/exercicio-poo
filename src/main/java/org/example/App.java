package org.example;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        int num = 5; // Número que queremos calcular o fatorial
        System.out.println("O fatorial de " + num + " é: " +  );
    }

    public static fatoriar(int num, int count){
       int count == num;
        for (int i = num; i > 0; i--) {
            num * i = count;

            return num;

        }
    }
}
